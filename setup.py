from setuptools import setup

setup(
   name='flight_data_manager',
   version='0.2.0',
   description='TODO',
   author='Ryan Hiatt',
   author_email='@gmail.com',
   packages=['flight_data_manager'],  # same as name
   install_requires=['wheel', 'kivy', 'pytest']  # external dependencies
)